package com.example.sunrin205.Internet

import com.example.sunrin205.data.LunchAndDate
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.GlobalScope
import kotlinx.coroutines.launch
import kr.go.neis.api.Menu
import kr.go.neis.api.School
import java.nio.file.Files.size

class ReturnData {
    /*fun returnLunchList(year: Int, month: Int): List<Menu>{
        var menu : List<Menu> = List()

        GlobalScope.launch(Dispatchers.IO) {
            val school = School(School.Type.HIGH, School.Region.SEOUL, "B100000658")
            menu =school.getMonthlyMenu(year, month)
        }
        return menu
    }*/
}